export interface NavbarLink {
    imgURL: string;
    route: string;
    label: string;
  }

  export interface MenuItem {
    name: string;
    image: string;
    description: string;
    isVegan: boolean;
    price: number;
  }